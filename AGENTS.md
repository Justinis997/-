# AGENTS.md instructions

## 1. 工作原则

接到任务后，先理解目标和相关上下文，再列出简明计划。不确定的地方直接问用户，不要自行猜测或扩大任务范围。

同一个错误不要犯第二次。用户纠正过的做法，记录到第 3 节，并在后续任务中遵守。

## 2. 语言与表达

除非用户特别要求使用其他语言，否则所有回复都使用简体中文。

回复应直接、清楚、实用。涉及文件、路径、代码、组件或函数时，尽量给出具体名称。

## 3. 错误纠正记录

- 网站所有双语栏目标题中，英文标题必须放在中文标题正下方，不得与中文标题横向并排。
- 摄影页面的作品网格必须保留适度间距和呼吸感，不得做成紧凑无缝拼贴。
- 关于我页面只有页面标题采用中英文双语，其余个人信息全部使用中文。
- 禁止使用、安装或调用任何 `superpowers` 技能及其工作流；项目任务应直接按本文件规则执行。
- 首页三个完整内容区块在桌面端宽度统一为屏幕宽度的 80%；高度应让当前区块完整呈现，并在屏幕底部刚好露出下一个区块的完整中英文标题。调整尺寸时还要同步约束摄影内容，避免图片最小高度把区块重新撑大或导致照片比例失衡。
- 首页页脚的小红书链接必须使用黑色圆角方块、白色“小红书”字样的品牌图标，不得使用普通文字代替 Logo。
- 首页页脚社交图标使用 19.2px 尺寸；图标组作为整体只做页面底部水平居中，不设置垂直居中规则，也不得再与内容板块右边界对齐。图标底边距离页面底部在桌面端为 20px，手机端为 15px。
- 首页最新摄影图片和最新文章条目点击后必须在当前页面显示完整内容，不得跳转到摄影页或文章页。
- 首页与摄影页面的灯箱翻页按钮必须共用一致的图标设计，左右箭头需在按钮内水平、垂直居中，不得使用容易产生字体偏移的字符箭头。
- 修改首页与摄影页面共用的灯箱视觉时，必须分别验证两个页面的实际渲染结果，并考虑旧页面 HTML 与新 CSS 同时存在的缓存状态；不得只检查代码结构后宣称完成。
- 导航栏左右必须保留参考图式的呼吸空间，桌面端使用约 `3vw` 的水平边距；摄影、文章和“关于我”子页面标题必须与导航内容共用同一条左右基线。
- 摄影页面照片的进入动效必须采用与首页内容区块一致的渐入上浮效果，不得只做透明度渐入；照片按行依次出现，最上面一行最先显示。
- 摄影、随笔和“关于我”三个子页面的中英文主标题必须以摄影页面为基准，同时统一左侧基线和顶部偏移；页面专用布局不得单独覆盖标题位置。
- 导航栏“摄影”和“随笔”使用参考 OpenAI 官网的全宽纯文字下拉菜单；摄影下拉显示“探索摄影”和四个分类，随笔下拉以“时间 / 类别”为两个大标题，并在悬停时切换对应的年份或类别二级标题；两级文字必须保持清楚的字号层级和一致的下拉动效。
- 桌面端导航下拉区域保持紧凑，但不得强制统一高度，必须根据摄影分类、随笔时间或随笔类别的实际内容自适应伸缩；下拉打开时，导航栏和下拉菜单保持清晰，菜单以外的页面区域使用灰化模糊遮罩，手机端不得启用该遮罩。
- 导航下拉菜单的大标题之间必须保留清楚的纵向呼吸空间，不得为了压缩下拉高度把摄影分类或随笔“时间 / 类别”标题挤在一起。
- 导航下拉菜单只有左侧“探索摄影”到“建筑”以及“浏览随笔”到“时间”使用 `16px` 间距；摄影分类大标题之间、随笔“时间 / 类别”之间保持 `8px`，随笔右侧二级标题与年份或分类之间也保持 `8px`。
- 导航下拉打开后的页面遮罩使用轻量模糊效果：`blur(9px)`、灰色遮罩透明度 `0.21`、饱和度 `87.5%`；不得恢复为此前过强的模糊效果。
- 导航下拉菜单只有“探索摄影”和“浏览随笔”两个左侧栏目说明使用 Light（`font-weight: 300`）；随笔右侧“时间 / 类别”等栏目说明保持 `font-weight: 600`，字体类型、字号和颜色不变。
- 首页“最新随笔”每篇卡片的内容顺序固定为标题、日期、正文；正文最多展示三行，溢出显示省略号，并从上到下呈现由清晰黑色逐渐过渡为轻微模糊灰色的效果。
- 首页“最新随笔”三张卡片的标题统一以“拧巴的人”的单行高度为基准；标题只能占一行，字数超出卡片宽度时使用省略号，不得换行，并确保下方日期和正文水平对齐。
- 首页“最新随笔”存在正文为空的卡片时，仍须保留与其他卡片相同的三行正文区域高度；所有卡片必须从顶部开始排列，不能因正文为空而把标题和日期垂直居中。
- 首页三个大区块在桌面端的左边界必须与导航栏“摄影”文字左边界对齐，并以该左侧距离为基准设置相同的右侧页面边距；调整首页宽度或边界时不得破坏最新摄影照片的圆角、裁切和网格比例。
- 摄影灯箱的左右翻页箭头必须脱离照片尺寸和说明文字布局，始终固定在整个页面的垂直中轴线上；切换不同横竖比例照片时不得上下移动。
- 摄影页面网格照片统一使用横版 `4:3` 比例，不得使用竖版 `3:4`；分类文字与第一行照片之间的距离使用原间距的一半，即 `clamp(22px, 3vw, 40px)`。
- 摄影页面照片网格的纵向行距使用较克制的留白：桌面端为 `clamp(35px, 3.33vw, 59px)`，平板端为 `24px`，手机端为 `19px`；不得恢复到此前过大的行距。
- 摄影页面按时间排序时，“时间”只能使用照片元数据中的创建时间（`creation`），不得使用文件导入时间、文件修改时间或文件名日期代替；有创建时间的照片从新到旧排列，无法读取创建时间的照片排在最后并保持未确认状态。
- 导航栏左侧不得显示图片 Logo，统一使用加粗文字“Justin”作为返回首页的品牌链接。
- “关于我”页面的照片左边缘必须与页面主标题左边缘对齐，不得在左侧网格内水平居中。
- “关于我”信息区只保留一条连续的竖向分割线，不使用横向分割线或多段断开的竖线。
- “关于我”信息区左侧标题与右侧正文之间的留白为 `42px`，分割线两侧各为 `21px`。
- “关于我”信息区的竖向分割线必须位于左侧标题与右侧正文留白的正中间，顶端与第一个标题“教育经历”的文字顶端对齐，不得向上超出。
- “关于我”信息区各行的纵向间距以标题和正文为同一行同步调整，当前基准为之前间距增大约 `30%`。
- “关于我”信息区左侧标题必须保持左对齐，调整分割线位置时不得擅自改为右对齐。
- 首页“最新消息”左侧栏目标题使用“作品更新”，不得写成“最近更新”；“近期动态”下方内容属于正文，必须使用克制的正文字号和常规字重，不得做成大标题视觉。
- 首页“最新消息”双栏内部不使用分割线；作品更新数字采用克制字号，数字下方说明文字同比例缩小，并与对应数字居中对齐。
